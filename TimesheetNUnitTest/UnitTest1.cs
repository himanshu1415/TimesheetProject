using NUnit.Framework;
using System.Collections.Generic;
using System.Linq;


namespace TimesheetNUnitTest
{
    public class Tests
    { /*
        public List<Employee> obj = new List<Employee>()
        {  new Employee{   EmployeeId="", EmployeeName="",EmployeeEmail="",EmplyoeeGender="" }
        };
        [SetUp]
        public void Setup()
        {
            var details = obj.AsQueryable();
            var mockset = new Mock<Employee>();
            mockset.As<IQueryable<Employee>>().Setup(m => m.Provider).Returns(details.Provider);
            mockset.As<IQueryable<Employee>>().Setup(m => m.Expression).Returns(details.Expression);
            mockset.As<IQueryable<Employee>>().Setup(m => m.ElementType).Returns(details.ElementType);
            mockset.As<IQueryable<Employee>>().Setup(m => m.GetEnumerator()).Returns(details.GetEnumerator());
        }

        [Test]
        public void Test1()
        {
            Assert.Pass();
        } */
    }
}